# Environment / secrets
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example
*.pem

# Local TLS material (generate with deploy/generate_selfsigned_cert.sh — never commit)
deploy/certs/*.crt
deploy/certs/*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Django
backend/db.sqlite3
backend/db.sqlite3-wal
backend/db.sqlite3-shm
backend/staticfiles/
backend/media/
*.log
logs/

# Test / coverage artifacts
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local DB dumps / operator backups (never commit)
backups/

# Transient git safety dumps from commit workflow
git_status_before_commit.txt
git_status_review.txt

# Deleted bridge parity helpers (keep ignored if a local lock prevents unlink)
scripts/parity_*.py

# Node
frontend/node_modules/
frontend/dist/
node_modules/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# OS / IDE / local agent
.DS_Store
Thumbs.db
.idea/
.vscode/
.cursor/
node_modules
