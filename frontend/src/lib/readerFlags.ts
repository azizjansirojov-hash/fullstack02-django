/**
 * React immersive reader is the only implementation.
 * Kept as a named helper for call sites and tests; always returns true.
 */
export function isReactReaderEnabled(): boolean {
  return true
}
